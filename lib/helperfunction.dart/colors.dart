import 'package:flutter/material.dart';

class AppColors {
  // ✅ Primary brand colors
  static const Color primary = Color(0xFF6A5AE0); // Example purple
  static const Color primaryDark = Color(0xFF4A3FB8);
  static const Color primaryLight = Color(0xFF8F83E5);
  static const Color appBarColor =  Color(0xff252526);
  static const Color cardColor = Color(0xff252526); 
  // ✅ Secondary or accent colors
  static const Color secondary = Color(0xFFFAB700); // Example yellow
  static const Color secondaryDark = Color(0xFFE0A100);

  // ✅ Neutral colors 
  static const Color background = Color(0xFF000000);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF757575);

  // ✅ Error colors
  static const Color error = Color(0xFFD32F2F);

  // ✅ Success & info
  static const Color success = Color(0xFF388E3C);
  static const Color info = Color(0xFF1976D2);

  // ✅ Divider or borders
  static const Color border = Color(0xFFE0E0E0);
}
